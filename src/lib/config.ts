import type { ConfigType } from "@/lib/configtypes";

export const pageConfig: ConfigType = {
	new_account: {
		add_transferable_coins: true,
		add_coins: false,
		add_vip_days: true,
		coins_transferable: 500,
		coins: 0,
		vip_days: 4,
	},
};
